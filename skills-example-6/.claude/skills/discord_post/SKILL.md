---
name: discord_post
description: 送信メッセージ(message) を、Discordに送信する。
---

## 手順

### STEP 1: 送信メッセージ(message)を確認する

* ユーザーに、送信メッセージ(message)を質問する:

```
送信メッセージ(message) を入力してください。
例: hello
```

### STEP 2: python スクリプトを実行する

```
python scripts/discord_post.py --message {message}
```

